<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="automation_cong_viec_done" model="base.automation">
        <field name="name">Tự động cập nhật dự án khi công việc hoàn thành</field>
        <!-- Model theo dõi: công việc -->
        <field name="model_id" ref="nquan_ly_cong_viec.model_cong_viec"/>
        <!-- Trigger: khi cập nhật bản ghi -->
        <field name="trigger">on_write</field>
        <!-- Điều kiện: chỉ kích hoạt khi trạng thái được chuyển thành 'hoan_thanh' -->
        <field name="filter_domain">[('trang_thai','=','hoan_thanh')]</field>
        <!-- Loại hành động: chạy code Python -->
        <field name="state">code</field>
        <!-- Code xử lý: kiểm tra dự án và cập nhật nếu tất cả công việc hoàn thành -->
        <field name="code">
for task in records:
    project = task.du_an_id
    if project:
        # Tính lại tiến độ dự án (gọi hàm đã có)
        project._tinh_tien_do()
        # Kiểm tra nếu tất cả công việc của dự án đã hoàn thành
        if project.tong_so_cong_viec > 0 and project.so_cong_viec_hoan_thanh == project.tong_so_cong_viec:
            if project.trang_thai != 'hoan_thanh':
                project.write({'trang_thai': 'hoan_thanh'})
        </field>
        <field name="active">True</field>
    </record>
</odoo>