from odoo import models, fields

class AIAssistant(models.TransientModel):
    _name = 'ai.assistant'
    _description = 'AI Assistant (transient model for future use)'
    # Có thể dùng để lưu lịch sử chat, nhưng tạm thời không dùng
    pass